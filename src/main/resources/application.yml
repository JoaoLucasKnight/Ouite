spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ouite
    username: root
    password: 140930
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
